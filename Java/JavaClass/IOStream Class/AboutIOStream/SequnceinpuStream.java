package ch15;

import java.io.SequenceInputStream;

public class SequnceinpuStream {
//SequenceInputStream은 여러개의 입력스트림을 연속적으로 연결해서 하나의 스트림으로부터 데이터를 읽는 것과 같이 도와준다.
//	생성자르 ㄹ제외한 다른 작업은 크게 다르지 않다.
//	큰 파일을 여러개의 작은 파일로 나웠다가 하나의 파일로 합치는 것과 같은 작업을 수행할 때 사용하면 좋다.
	
//	SequnceInputStream은 FilterInputStream의 자손이 아닌 InputStream의 바로 상속 이다.

	//생성자
//	SequenceInputStream sis = new SequenceInputStream(e)
	//두 입력 스트림을 하나로 연결한다.
//	SequenceInputStream sis2 = new SequenceInputStream(s1, s2)



}