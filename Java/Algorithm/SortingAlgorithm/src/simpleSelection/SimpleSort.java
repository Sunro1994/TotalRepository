package simpleSelection;

public class SimpleSort {
	public static void main(String[] args) {
		//가장 작은 요소부터 선택해 알맞은 위치로 옮겨서 순서대로 정렬하는 알고리즘
		// 아직 정렬하지 않은 부분에서 값이 가장 작은 요소를 선택하고 아직 정렬하지 않은 부부분의 첫 번째 요소와 교환한다.
		
		
		
		//교환과정
		//1. 아직 정렬하지 않은 부분에서 가장 작은 키의 값(a[min])을 선택한다.
		//2. a[min]과 아직 정렬하지 않은 부분의 첫번째 요소를 교환한다.
		
//		for(int i=0; i<n-1; i++) {
			//min = a[i]... , a[n-1]에서 가장 작은 값을 갖는 요소의 인덱스
			//a[i] 와 a[min]의 값을 교환
			
//		}
		
		
	}
}
