-- 코드를 입력하세요
SELECT 
FOOD_TYPE,
REST_ID,
REST_NAME,
FAVORITES
FROM 
REST_INFO A
RIGHT
JOIN
(SELECT 
 FOOD_TYPE      AS FT,
 MAX(FAVORITES) AS FS
 FROM REST_INFO 
 GROUP BY FOOD_TYPE
 ) B
 ON A.FOOD_TYPE = B.FT
 WHERE FAVORITES = FS
 ORDER BY FOOD_TYPE DESC;
 

     
     


